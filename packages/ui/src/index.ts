/**
 * @mercatus/ui -- design tokens and the small shared component set (BUILD-PLAN §7.1, task 04). Plain CSS: no component library, no Tailwind, no CSS-in-JS.
 *
 * Deliberately empty: the package exists now so the workspace wiring, tsconfig and lint config
 * are proven before anyone writes a line of it.
 */
export {};
